export interface Ticket_Booking_DataInterface {
  train_name: string,
  time: string,
  start: string,
  departure: string,
  created: string,
  last_update: string,
  status: string,
  my_reservation: string,
  action: string
}